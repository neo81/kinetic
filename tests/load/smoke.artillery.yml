config:
  target: "http://127.0.0.1:3000"
  phases:
    - duration: 30
      arrivalRate: 2
      name: "smoke"
  ensure:
    thresholds:
      - http.response_time.p95: 1000
      - http.codes.200: 1

scenarios:
  - name: "app shell"
    flow:
      - get:
          url: "/"
